# Supabase project configuration.
#
# Aucune Edge Function n'est utilisée actuellement : le checkout est simulé
# côté client (voir hooks/useMockCheckout.ts) et insère directement la commande
# en base via le client Supabase.
#
# Pour ajouter une Edge Function plus tard (ex. paiement Stripe réel) :
#   supabase functions new <nom>
# puis ajouter ici la section [functions.<nom>] correspondante.
